Note: to activate / deactivate a py environment:

source py3/bin/activate / deactivate

To build:

update version in setup.py

python -m build

To push to pip:

twine upload dist/semanticanalyser_py-0.1.x*